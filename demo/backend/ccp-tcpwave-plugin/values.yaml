global:
  repository: &repository coredgeio
  releaseTag: &releaseTag latest
  imagePullPolicy: &imagePolicy Always
  storageClassName: ""

replicaCount: 1

image: 'ccp-tcpwave-plugin'

app:
  orbiter_metering_base_uri: "http://orbiter-metering:8081"
  orbiter_event_enabled: "true"
  mongo_db_name: "ccp_dns_management"
  compass_api_url: "http://compass-api:8085"

  mongoUrlOverride: "mongodb://sigma:pass@compass-configdb:27017"

  resources: 
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 500m
      memory: 512Mi

# HPA Configuration
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80