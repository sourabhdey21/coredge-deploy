apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: coredge-prod-frontend
  namespace: argocd
  labels:
    customer: coredge
    env: prod
    category: frontend
spec: 
  generators:
    - list:
        elements:
          - app: admin-portal
          #- app: celery-flower
          - app: user-portal
          - app: mongo-express
          - app: pgadmin
  template: 
    metadata: 
      name: '{{app}}-coredge-prod'
      namespace: argocd
      labels:
        customer: coredge
        env: prod
        category: frontend
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: coredge-prod
      # values repo first so it shows in argocd UI
      sources:
        - repoURL: https://github.com/sourabhdey21/coredge-deploy.git
          targetRevision: master
          ref: values
        - repoURL: https://github.com/sourabhdey21/ccs-helm-charts.git
          targetRevision: master
          path: helm/frontend-charts/{{app}}
          helm:
            valueFiles:
              - $values/prod/images.yaml
              - $values/prod/frontend/{{app}}/values.yaml
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
