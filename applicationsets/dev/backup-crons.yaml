apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: backup-crons-dev
  namespace: argocd
  labels:
    customer: ccs
    env: dev
    category: backup-crons
spec:
  generators:
    - list:
        elements:
          - app: keycloak-postgres
          - app: ccs-postgres
          - app: mongodb
          - app: openfga
          - app: etcd
  template:
    metadata:
      name: '{{app}}-backup-cron-dev'
      namespace: argocd
      labels:
        customer: ccs
        env: dev
        category: backup-crons
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: coredge-dev
      sources:
        # 1) values repo
        - repoURL: https://github.com/sourabhdey21/coredge-deploy.git
          targetRevision: master
          ref: values
        # 2) helm charts repo
        - repoURL: https://github.com/sourabhdey21/ccs-helm-charts.git
          targetRevision: master
          path: helm/backup-crons/{{app}}
          helm:
            valueFiles:
              - $values/dev/backup-crons/{{app}}/values.yaml
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
