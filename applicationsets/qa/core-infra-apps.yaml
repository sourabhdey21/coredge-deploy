apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: coredge-qa-core-infra
  namespace: argocd
  labels:
    customer: coredge
    env: qa
    category: core-infra
spec: 
  generators:
    - list:
        elements:
          - app: kafka
          - app: mongodb
          - app: openfga
          - app: postgres
          - app: redis
          - app: keycloak-ha
          - app: repository-cred
  template: 
    metadata: 
      name: '{{app}}-coredge-qa'
      namespace: argocd
      labels:
        customer: coredge
        env: qa
        category: core-infra
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: coredge-qa
      # values repo first so it shows in argocd UI
      sources:
        - repoURL: https://github.com/sourabhdey21/coredge-deploy.git
          targetRevision: main
          ref: values
        - repoURL: https://github.com/sourabhdey21/ccs-helm-charts.git
          targetRevision: main
          path: helm/core-infra-charts/{{app}}
          helm:
            valueFiles:
              - $values/qa/images.yaml
              - $values/qa/core-infra/{{app}}/values.yaml
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
